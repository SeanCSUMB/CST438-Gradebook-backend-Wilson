server.port = 8081
server.servlet.session.persistent=false

# registration service
registration.url = http://localhost:8080
registration.service = default
gradebook.service = default

# database properties
spring.datasource.url=jdbc:mysql://localhost:3306/gradebook
spring.datasource.username=root
spring.datasource.password=sesame80
spring.jpa.hibernate.ddl-auto=update

# URL for redirect back to React after successful login
frontend.post.login.url = http://localhost:3000/assignment
# google authentication provider
spring.security.oauth2.client.registration.google.client-id = //The assignment recommends not having the id and secret in the public repository,
spring.security.oauth2.client.registration.google.client-secret = //so they have been removed.

#server.servlet.session.persistent=false
#gradebook.service = default
#registration.service = MQ 
#spring.rabbitmq.addresses=${CLOUDAMQP_URL}
#spring.jpa.hibernate.ddl-auto = update
#spring.datasource.hikari.maximum-pool-size=5